{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "frontend",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo Building frontend"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo Serving frontend"
      }
    }
  }
}
