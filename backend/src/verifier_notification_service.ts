export function notifyVerifier(userId: string, credentialId: string, direction: 'left' | 'right'): void {
  // Placeholder for sending a notification to the credential verifier
  console.log(`Notify verifier: user ${userId} swiped ${direction} on credential ${credentialId}`);
}
