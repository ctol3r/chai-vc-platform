// Prisma schema for chai-vc-platform
// Using SQLite database stored in dev.db

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Credential {
  id    Int    @id @default(autoincrement())
  title String
}
