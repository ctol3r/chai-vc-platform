Docker Compose for staging (staging/deploy branch).
- Use `docker compose up --build -d` from repo root.
- Use `scripts/seed.sh` after services are healthy to seed demo data (edit mutation as needed).
